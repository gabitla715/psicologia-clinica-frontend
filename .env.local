# Backend corriendo localmente (Spring Boot, puerto por defecto 8080)
# OJO: incluye /v1 porque el AuthController real está mapeado en /api/v1/auth
VITE_API_URL=http://localhost:8080/api/v1
VITE_USE_MOCK=false
